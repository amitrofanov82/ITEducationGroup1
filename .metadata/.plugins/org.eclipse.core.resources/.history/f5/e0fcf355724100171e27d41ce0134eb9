package teacher.homework.lesson8;
public class DragonBlankForItStep {
	// Это будет следующей новой темой. Сейчас не нужно:
	public final static int DRAGONINITHEALTH = 0; //задать значения из задания, public делать не нужно!!!
	private static int atackD ;
	private static int healthD = 0;
	private static int healthK = 0;
	private static int atackK = 0;
	private static int B = 0;
	private static int D = 0;
	//static int healthD;
	//static int initHealthD;
	//static int atackD;
	//static int healthK;
	//static int atackK;
	//static int B; 
	//static int D;
	
	
	public static void main(String[] args) {
		
		/*int healthD = 0; //задать значения из задания
		int atackD = 0;
		int healthK = 0;
		int atackK = 0;
		int B = 0;
		int D = 0;*/
		int turns = 0; //номер раунда
		int actionNumber;
		
		printCurrentstat(turns, healthD, atackD, healthK, atackK);
		boolean end = false;
		while (!end){
			actionNumber = choosedAction(healthD, atackD, healthK, atackK, B, D);
			/*//ниже было бы удобнее switch(actionNumber){}, но это тоже будет новой темой. С ифами тоже всё можно сделать:
			if (actionNumber == 1) { //например 1 значит, что выбран удар по рыцарю. А 2 - что дракон решил излечиться
				//...
			}
			if (actionNumber == 2) {
				//...
			}
			if (actionNumber == 3) {
				//...
			}
			if (actionNumber == 4) {
				//...
			}*/
		//	switch(actionNumber){
			switch(choosedAction(healthD, atackD, healthK, atackK, B, D)){
			case 1:
				//ударить
				break;
			case 2:
				//лечиться
				break;
			case 3:
				//баф
				break;
			case 4:
				//дебаф
				break;
				default://не обязателен
					System.out.println("так не должно быть");
			}
			//проверить не конец ли...
			//healthD = knightAtack(healthD, atackK);
			kinghtAtack();
			//...
			//if (turns > 2000) break; вдруг у вас зациклится, что долго никто не помирает.
		}
		//вывести результат
	}


	private static int knightAtack(int healthD, int atackK) {
		//...
		return 0;
	}
	
	private static void knightAtack() {
		//...
		return;
	}

	private static int choosedAction(int healthD, int atackD, int healthK, int atackK, int b, int d) {
		//...
		return 0;
	}

	private static int buff(int atackD, int B) {
		//...
		return 0;	
	}

	private static int debuff(int atackK, int D) {
		//...
		return 0;	
	}
	
	private static void printCurrentstat(int turns, int healthD, int atackD, int healthK, int atackK) {
		//...
	}
	

}